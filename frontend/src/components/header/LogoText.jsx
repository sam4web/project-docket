import { Link } from "react-router-dom";

const LogoText = () => {
  return (
    <Link to="/">
      <h2 className="text-[21px] text font-semibold text-responsive select-none cursor-pointer">
        docket
      </h2>
    </Link>
  );
};

export default LogoText;